# Dependencies
node_modules/

# Next.js / build output
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------------
# frontend/ currently holds stale Next.js *build output* (the contents of a
# .next/ directory) rather than source, so it is ignored here to stop it being
# committed and pushed. When the real frontend SOURCE is added, delete this
# line (and `git rm -r --cached frontend` has already untracked the old files).
# ---------------------------------------------------------------------------
/frontend/
