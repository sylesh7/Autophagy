# ---------------------------------------------------------------------------
# Autophagy frontend configuration
# ---------------------------------------------------------------------------

# Where the BFF proxy (app/api/autophagy/[...path]/route.ts) forwards requests.
# Server-side only — the browser never sees this. Defaults to the backend's
# own local default port.
AUTOPHAGY_API_URL=http://127.0.0.1:8080
