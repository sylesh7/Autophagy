{
  "name": "autophagy-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "EfficiencyRegistry — public efficiency reputation for autonomous agents",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test mocha",
    "deploy": "hardhat ignition deploy ignition/modules/EfficiencyRegistry.ts --network baseSepolia",
    "deploy:local": "hardhat ignition deploy ignition/modules/EfficiencyRegistry.ts --network hardhatOp",
    "verify": "hardhat verify --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.15",
    "@nomicfoundation/hardhat-ignition": "^3.1.8",
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.7",
    "@types/node": "^26.4.0",
    "ethers": "^6.17.0",
    "hardhat": "^3.15.0",
    "typescript": "~6.0.3"
  }
}
